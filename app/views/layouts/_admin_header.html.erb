<nav class="navbar navbar-expand-lg navbar-light bg-dark">
  <div class="navbar-brand text-white">ぐ～たライフ</div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if admin_signed_in? %>
        <li>
          <%= link_to admin_rests_path, class: "nav-link text-white" do %>
            <i class="fas fa-address-book"></i>投稿一覧
          <% end %>
        </li>
         <li>
          <%= link_to admin_customers_path, class: "nav-link text-white" do %>
            <i class="fas fa-users"></i>ユーザー一覧
          <% end %>
        </li>
        <li>
          <%= link_to destroy_admin_session_path, method: :delete, class: "nav-link text-white" do %>
            <i class="fas fa-door-closed"></i>ログアウト
          <% end %>
        </li>
      <% else %>
        <li>
          <%= link_to new_admin_session_path, class: "nav-link text-white" do %>
            <i class="fas fa-door-open"></i>ログイン
          <% end %>
        </li>
      <% end %>
    </ul>
    <div class="form-group my-2 my-lg-0 form-inline">
      <%= search_form_for @search, url: admin_search_path do |f| %>
      <%= f.search_field :address_cont, class:"form-control mr-sm-2", placeholder:"住所を入れてください" %>
    </div>
    <div class="actions"><%= f.submit "search", class:"btn btn-outline-success" %></div>
      <% end %>
  </div>
</nav>